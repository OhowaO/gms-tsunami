version: '2'
services:
  gms-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/GMS/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=GMS
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
